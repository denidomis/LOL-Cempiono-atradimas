import React, { useState, useEffect, useRef } from "react";
import { useTable } from "react-table";
import Calculator from "./Calculator";
import "./index.css";

// //const Weighted1 = DP * 5 + TP * 16 + CCP * 10 + MP * 2 + UP;
// const Weighted2 = DP * 2 + TP * 9 + CCP * 13 + MP * 2 + UP;
// const Weighted3 = DP * 9 + TP * 6 + CCP * 8 + MP * 2 + UP;
// const Weighted4 = DP * 4 + TP * 4 + CCP * 14 + MP * 2 + UP;
// const Weighted5 = DP * 11 + TP * 0 + CCP * 5 + MP * 2 + UP;
// const Weighted6 = DP * 10 + TP * 1 + CCP * 3 + MP * 2 + UP;
// const Weighted7 = DP * 8 + TP * 10 + CCP * 4 + MP * 2 + UP;
// const Weighted8 = DP * 15 + TP * 15 + CCP * 10 + MP * 2 + UP;
// const Weighted9 = DP * 3 + TP * 14 + CCP * 10 + MP * 2 + UP;
// const Weighted10 = DP * 4 + TP * 13 + CCP * 10 + MP * 2 + UP;
// const Weighted11 = DP * 1 + TP * 10 + CCP * 10 + MP * 2 + UP;
// const Weighted12 = DP * 0 + TP * 17 + CCP * 10 + MP * 2 + UP;

// const [Weighted1, setWeighted1] = useState("");
// useEffect(() => {
//   setWeighted1(DP * 5 + TP * 16 + CCP * 10 + MP * 2 + UP);
// }, [DP, TP, CCP, MP, UP]);

const Tables = () => {
  return <div></div>;
};
export default Tables;
